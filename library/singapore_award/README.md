# singapore.award_search

Starter script scaffold for Singapore Airlines award travel.

- Replace `runner.py` with real OpenClaw interactions.
- Keep manifest and schemas updated for contract checks.
- Set `cabin` to `economy` for baseline scans.
- Use `credential_refs` + webhook 2FA handoff; do not pass raw credentials.
