# Weather Check Example

No-login weather query using Open-Meteo.

## Run

```bash
python -m openclaw_automation.cli run \
  --script-dir examples/weather_check \
  --input '{"location":"San Francisco, CA","temperature_unit":"fahrenheit"}'
```
