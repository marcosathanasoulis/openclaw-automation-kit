{
  "setupComplete": false,
  "prerequisites": {
    "python": {
      "required": true,
      "installed": false,
      "description": "Python 3.10+"
    },
    "packageInstalled": {
      "required": true,
      "installed": false,
      "description": "openclaw-automation-kit installed locally (pip install -e .)"
    },
    "psycopg": {
      "required": true,
      "installed": false,
      "description": "psycopg with binary extras for PostgreSQL connectivity"
    },
    "postgresDsn": {
      "required": true,
      "configured": false,
      "description": "DATABASE_DSN or explicit --dsn value"
    }
  },
  "setupInstructions": [
    "1. Install package: pip install -e .",
    "2. Install DB driver: pip install \"psycopg[binary]>=3.2\".",
    "3. Ensure Postgres has pgvector extension enabled.",
    "4. Initialize schema via run_memory.py --action init-schema."
  ],
  "verifySetup": "python skills/openclaw-universal-memory/scripts/run_memory.py --action init-schema --dsn \"$DATABASE_DSN\""
}
